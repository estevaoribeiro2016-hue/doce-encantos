# Doce Encanto V57.3 — Central revisada

Correções:
- variável `canceled` definida antes da renderização da aba Cancelados;
- revisão das variáveis `pending`, `delivered`, `canceled`, `history` e `production`;
- separação entre erro de autenticação e erro ao renderizar a Central;
- atualização do cache para impedir carregamento do JavaScript antigo.

Não exige novo SQL no Supabase.
