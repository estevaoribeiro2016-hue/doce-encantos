"use client";export default function StatusButtons({id}:{id:string}){async function set(status:string){await fetch(`/api/orders/${id}/status`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})});location.reload()}return <div className="flex gap-2 mt-3 flex-wrap"><button className="btn" onClick={()=>set('READY')}>Pedido pronto</button><button className="btn" onClick={()=>set('OUT_FOR_DELIVERY')}>Saiu para entrega</button><button className="btn" onClick={()=>set('DELIVERED')}>Entregue</button><button className="btn" onClick={()=>set('CANCELED')}>Cancelar</button></div>}
