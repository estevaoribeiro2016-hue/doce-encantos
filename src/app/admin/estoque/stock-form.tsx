"use client";import {useState} from 'react';export default function StockForm({productId}:{productId:string}){const[quantity,setQuantity]=useState('');async function save(){await fetch('/api/stock',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({productId,quantity:Number(quantity),reason:'Ajuste manual no painel'})});location.reload()}return <div className="flex gap-2 mt-2"><input className="border rounded p-2" placeholder="Nova quantidade" value={quantity} onChange={e=>setQuantity(e.target.value)}/><button className="btn" onClick={save}>Salvar estoque</button></div>}
